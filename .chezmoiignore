# Secrets - NEVER in the repo
.npm_token
.envrc
*.key
*.pem
*credentials*
*secret*

# Git directories inside configs (e.g., nvim has its own repo)
**/.git
**/.git/**

# Files generated by applications
.config/nvim/lazy-lock.json
.config/fish/fish_variables
.config/atuin/history.db
.config/zellij/plugins/*.wasm

# Backup files
*.bak
*.bak.*

# OS specific ignores
{{ if ne .chezmoi.os "darwin" }}
# Linux ignores macOS specific files
{{ end }}
{{ if ne .chezmoi.os "linux" }}
# macOS ignores Linux specific files
{{ end }}
