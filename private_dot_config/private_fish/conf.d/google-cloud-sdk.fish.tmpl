# Google Cloud SDK — PATH for fish (equivalent to the path.bash.inc sourced from .bashrc)
if test -f {{ .chezmoi.homeDir }}/google-cloud-sdk/path.fish.inc
    source {{ .chezmoi.homeDir }}/google-cloud-sdk/path.fish.inc
end
